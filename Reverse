#include<iostream>
#include<string.h>
using namespace std;
int main()
{
    char s[20];
    int i,len;
    cout<<"enter the String:";
    cin>>s;
    len=strlen(s);
    for(i=len-1;i>=0;i--)
    {
        cout<<s[i];
    }

    
    
    return 0;
}
